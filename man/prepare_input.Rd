% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_input.R
\name{prepare_input}
\alias{prepare_input}
\title{Prepare THREAD model input}
\usage{
prepare_input(
  X,
  response,
  z_init = NULL,
  priors = NULL,
  gene2vec = NULL,
  K_init = 10,
  k_neighbors = 100,
  gene_col = NULL,
  response_col = "final_y",
  x_transform = c("none", "log2p1", "log1p"),
  center_x = FALSE,
  scale_x = FALSE,
  min_genes = 50,
  seed = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{X}{Gene-by-subtype expression matrix. Rows must be genes.}

\item{response}{Data frame with gene, final_y, d0, d1 and d2 columns.}

\item{z_init}{Optional initial labels. Can be a vector aligned to genes, a
named vector, or a data frame with gene and cluster columns.}

\item{priors}{Optional list with alpha0, beta0, r and delta. When NULL,
\code{recommend_priors()} is called.}

\item{gene2vec}{Optional gene2vec matrix/data frame/path used only when
\code{z_init} is NULL.}

\item{K_init}{Initial number of clusters when z_init is not supplied.}

\item{k_neighbors}{Number of neighbours for gene2vec-missing genes.}

\item{gene_col}{Optional gene column name in response and z_init data frames.}

\item{response_col}{Response column name. Default final_y.}

\item{x_transform}{Expression transform: none, log2p1 or log1p. Default none.}

\item{center_x}{Center expression columns before model fitting. Default FALSE.}

\item{scale_x}{Scale expression columns before model fitting. Default FALSE.}

\item{min_genes}{Minimum number of overlapping valid genes. Default 50.}

\item{seed}{Optional seed used by init_clusters.}

\item{verbose}{Logical.}

\item{...}{Reserved for future extensions.}
}
\value{
A list of class \code{thread_data} with x_train, y_train, d0, d1, d2,
z_init, gene_names and priors.
}
\description{
Aligns a gene-by-subtype expression matrix with a gene-level GWAS response,
creates or validates initial cluster labels, recommends prior hyperparameters
when they are not supplied, and returns a \code{thread_data} object consumed by
\code{run_thread()}.

The response is kept on the raw LD-corrected scale. No log transform or
truncation is applied to y, because y, d0, d1 and d2 must remain on the same
scale as the heteroscedastic likelihood.
}
